[project]
name = "ai-book-ingestor"
version = "4.0.0"
description = "Universal Arabic/English textbook REST ingestion, persistent background jobs, question intelligence, visual understanding and OpenSearch indexing for agentic AI teachers"
requires-python = ">=3.11"

[project.optional-dependencies]
mineru = ["mineru[all]>=3.0.0"]

[tool.pytest.ini_options]
pythonpath = ["."]
