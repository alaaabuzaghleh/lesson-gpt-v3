# Remote API (auth, catalog, search) — proxied as /remote-api in dev
VITE_REMOTE_API_BASE_URL=

# Local extractor API (PDF upload, jobs, SSE) — proxied as /api in dev
VITE_EXTRACTOR_API_BASE_URL=

# Legacy single-base override (leave empty to use dual proxies above)
# VITE_API_BASE_URL=
