[project]
name = "remote-lessons-gpt"
version = "4.0.0"
description = "remoteLessonsGPT — production API for auth, catalog, ingest, PostgreSQL, and OpenSearch"
requires-python = ">=3.11"

[project.optional-dependencies]
mineru = ["mineru[all]>=3.0.0"]

[tool.pytest.ini_options]
pythonpath = ["."]
