# API base URL (empty = use Vite dev proxy to localhost:8080 local extractor)
VITE_API_BASE_URL=

# Optional: remote production API for direct catalog/auth calls (usually proxied via local API when REMOTE_API_URL is set on the backend)
# VITE_REMOTE_API_BASE_URL=
